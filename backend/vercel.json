{
  "version": 2,
  "builds": [
    {
      "src": "./dist/index.js",
      "use": "cd /src/db && npx prisma generate && cd ../../ && tsc -b && @vercel/node"
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "/"
    }
  ]
}
